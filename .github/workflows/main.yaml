name: Deploy Prod
run-name: "Deploy Prod: ${{ github.ref_name }}"
on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
  workflow_dispatch:
jobs:
  deployProdJobs:
    runs-on: ubuntu-latest
    uses: ./github/workflows/deploy.yaml
    with:
      environment: prod
