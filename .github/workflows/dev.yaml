name: Deploy Dev
run-name: "Deploy Dev"
on:
  workflow_dispatch:
jobs:
  deployDevJobs:
    uses: ./.github/workflows/deploy.yaml
    with:
      ENVIRONMENT: Dev
    secrets:
      TEST_SEC: ${{ secrets.TEST_SEC }}
